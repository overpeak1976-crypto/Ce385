workshop1_1 = กำหนดชื่อตัวแปรและแสดงผลออกทางคอนโซน

workshop1_2 = กำหนดชื่อตัวแปรแล้่วใช้ typeof เพื่อหาชนิดข้อมูล

workshop1_3 = กำหนดชื่อตัวแปร หาผลรวม หาคะแนนเฉลี่ย

workshop1_4 = กำหนดตัวแปร 
if เช็คชื่อกับรหัสผ่านว่าตรงในตัวแปรมั้ยและอายุน้อยกว่าหรือเท่ากับ18 จะแสดงคอนโซนว่าเข้าสู่ระบบสำเร็จ
else if เช็คว่าตัวชื่อกับรหัสในตัวแปรตรงกันมั้ย ถ้าไม่จะแสดงบนคอนโซนว่า ชื่อผู้ใช้หรือรหัสสผ่านไม่ถูกต้อง
else if ถ้าอายุน้อยกว่า 18 แสดงผลบนคอนโซลว่า อายุไม่ถึงเกณฑ์